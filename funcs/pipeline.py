import streamlit as st

def app():
    st.title('Pipeline')
    st.write('Our AI pipeline consists of several stages: data collection, model training, and deployment.')
    st.write('We use state-of-the-art machine learning models to ensure high accuracy and performance.')